{
  "name": "recipemanager",
  "version": "1.0.0",
  "description": "A simple web-app to allow users to create recipes and search through them with a list of ingredients",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "fs": "^0.0.1-security"
  }
}
